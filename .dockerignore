# 构建上下文排除项：显著减小上下文、避免把本地产物带进镜像
target/
data/
.git/
.github/
tests/
examples/
*.md
!README.md
Dockerfile
docker-compose.yml
.dockerignore
config.json
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
core.*

# 注意：bin/（CI 下载的预构建二进制）**不能**排除 ——
# Dockerfile.ci 依赖它组装镜像。默认不在排除列表里，故放行。
